{% extends 'manager/manager-layout.html.twig' %}

{% block page %}

<div class="row">
	<div class="col-lg-12">
		<h1>Les contrats</h1>
		<hr>
	</div>
</div>

{% for file in finder %}
	<i class="fa fa-file-pdf-o" aria-hidden="true" style="color:red;"></i> <a href="{{ asset('documents/bills/') }}{{app.user.workspace.name}}/{{file.filename}}">{{file.filename}}</a> <br>
{% endfor %}

{% endblock page%}
