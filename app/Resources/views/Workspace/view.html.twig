{% extends "layout.html.twig" %}

{% block title %}
  {{ parent() }} - {{ws.name}}
{% endblock %}

{% block stylesheets %}
	
{% endblock %}

{% block body %}

<span id="wsName" hidden="true">{{ws.name}}</span>   {# Important, permet de router pour les comment et vote#}

<div class="container-fluid" id="wsHeader">
{# 	<div class="row text-center">
		<div class="col-lg-6">
			{% if pictureLogo[0] is defined %}
				<img src="{{ asset('images/Workspaces/') }}{{ws.name}}/{{pictureLogo[0].url}}" alt="{{pictureLogo[0].alt}}">
			{% else %}
				<h1>{{ws.name}}</h1>
			{% endif %}
		</div>
		<div class="col-lg-6">
			{{averageVote|rating}}
		</div>
	</div> #}
{############################################# Caroussel #########################################}
	<div class="row text-center">
		<div class="col-lg-9">
			{% include 'Workspace/carroussel2.html.twig' %}
		</div>	
		<div class="col-lg-3" id="carroussel-right">
			{% if pictureLogo[0] is defined %}
				<img src="{{ asset('images/Workspaces/') }}{{ws.name}}/{{pictureLogo[0].url}}" alt="{{pictureLogo[0].alt}}">
			{% else %}
				<h1>{{ws.name}}</h1>
			{% endif %}
			<br><br>
			{############################################# Map #########################################}
			{# La taille de la carte joue sur les options affichées : itinéraire... #}
			<div id="map">
				<iframe frameborder="0" style="border:0;width: inherit;" src="https://www.google.com/maps/embed/v1/place?q={{ws.name}} {{ws.street}} {{ws.postcode}} {{ws.city}}&key=AIzaSyACES16ClzyOdiVa9Ohd-_unkM5rvvbo7o&zoom=15" ></iframe>

			{# 	<a href="http://maps.google.fr/maps?f=q&hl=fr&q={{ws.name}} {{ws.street}} {{ws.postcode}} {{ws.city}}" target="_blank">
				<button type="button">Agrandir le plan</button></a> #}
			</div>
			<div id="contact">
				{{ws.street|capitalize}} <br>
				{{ws.postCode}} {{ws.city|capitalize}} <br>
			</div>
			<br>
			<div id="time">
				<center><i class="fa fa-clock-o fa-2x" aria-hidden="true"></i></center>
				{{ws.openHoursInfo}}<br>
				<a href="{{ws.website}}" target="_blank">{{ws.website}}</a>
				<button type="button"><i class="fa fa-envelope-o" aria-hidden="true"></i>Contacter le Manager</button>
			</div>
		</div>		
	</div>
{# 	<div class="row text-center" id="contact">
		{{ws.street}} - {{ws.postCode}} {{ws.city}}
	</div> #}
	{############################################# horaire #########################################}	
{# 	<div class="row text-center"  id="time">
		<i class="fa fa-clock-o fa-2x" aria-hidden="true"></i> {{ws.openHoursInfo}}
		<a href="{{ws.website}}" target="_blank">{{ws.website}}</a>
		<button type="button"><i class="fa fa-envelope-o" aria-hidden="true"></i>Contacter le Manager</button>
	</div> #}
</div>
{############################################# Description #########################################}
<div class="container-fluid">
		<div class="row text-center">
			<span id="bonus">{{ws.descriptionBonus}}</span>
		</div>
		<div class="row">
			<div class="col-lg-8" id="description">	
				<h2>{{ws.name}} {{averageVote|rating}}</h2>		
				<p>{{ws.description}}</p>
				<p>Sed laeditur hic coetuum magnificus splendor levitate paucorum incondita, ubi nati sunt non reputantium, sed tamquam indulta licentia vitiis ad errores lapsorum ac lasciviam. ut enim Simonides lyricus docet, beate perfecta ratione vieturo ante alia patriam esse convenit gloriosam.</p>
				{# <hr> #}
				
			</div>
			<div class="col-lg-4" id="services">
				<h2>Nos services</h2>
				<p>Sed laeditur hic coetuum magnificus splendor levitate paucorum incondita, ubi nati sunt non reputantium, sed tamquam indulta licentia vitiis ad errores lapsorum ac lasciviam. ut enim Simonides lyricus docet, beate perfecta ratione vieturo ante alia patriam esse convenit gloriosam.</p>
				<center>
				{% for amenity in ws.amenities %}
					<img src="{{ asset('images/icons/amenities/BsurN/') }}{{amenity.urlLogo}}" alt="{{amenity.name}}" title="{{amenity.name}}">
				{% endfor %}
				</center>
			</div>
		</div>
{############################################# Booking #########################################}
		<div class="row">
			<div id="booking">
				<h2>Réserver</h2>
				{{ render(controller('BecowoCoreBundle:Booking:bookForm', {name: ws.name})) }}
				{# {% include 'Workspace/book3.html.twig' %} #}
			</div>
		</div>
		<div class="row text-center">
{############################################# Team #########################################}
			<div class="col-lg-3" id="team">
				<h2>L'équipe à votre service</h2>
				{% include 'Workspace/teamMembers.html.twig' %} 
				{# {% for teamMember in ws.teamMember %}
					<div class="col-lg-2">
			        	<img src="{{ asset('images/Workspaces/') }}{{ws.name}}/{{teamMember.urlProfilePicture}}" alt="{{teamMember.urlProfilePicture}}">
			          	<h4>{{teamMember.firstname}} {{teamMember.name}}</h4>
			          	<span class="label label-info">{{teamMember.job}}</span>
			          	<p>{{teamMember.description}}</p>
		          	</div>
			    {% endfor %}#}
			</div>
		</div>
{############################################# Panel Event #########################################}
			<div class="row text-center">
				<h2>Evenements</h2>
				<p>Ne manquez pas les évènements chez {{ws.name}}</p>
				{% include 'Workspace/events.html.twig' %}
			</div>

{############################################# Commentaires #########################################}
		<div class="row">
			<div id="CommentResults">
				{{ render(controller('BecowoCoreBundle:Comment:view', {name: ws.name})) }}
			</div>
		</div>

</div>  {# end of container #}

{% block javascripts %}    
{% endblock javascripts %}

{% endblock body %}
